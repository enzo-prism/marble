# Marble 2.4 (60) TestFlight plan

Exact uploaded GitHub `main` source:
`b287d7238494799818db5947524a5cc05b9c8a9c`

App Store Connect build ID: `ef651ca3-451f-468b-903a-1239bcf6dc39`

Live state verified 2026-08-24: `VALID`, internal `IN_BETA_TESTING` in
`test group A`, external `READY_FOR_BETA_SUBMISSION`. Strict TestFlight
validation reports 0 errors, 0 warnings, 0 blockers, and 0 informational findings.
Exact-main CI `32704746418` and TestFlight workflow `32705379775` passed.

Build 60 is attached to App Store 2.4 and fresh submission
`5bd874ad-3619-4521-9231-fa45ee18a4b0` is `WAITING_FOR_REVIEW`. It is not
approved or publicly released; release type is manual and public production
remains 2.3 build 56.

The former build 59 submission `2233970e-2288-4bf4-a52f-8bc5c47f639a` was
canceled and is `COMPLETE`.

## What to Test (en-US)

Marble 2.4 build 60 is a focused replacement candidate for two release issues.
From the default Add tab, confirm Start or open workout is always available and
can start a session even when no workout is active. Then import a workout where
the library contains duplicate exercise names: confirm the celebration uses the
exact exercise selected during review, and that choosing Create New never reports
a personal record from an unrelated same-named exercise. Recheck Add parsing,
structured review, save, Log, Progress, rest, and the active-workout accessory.
Send feedback with the smallest workout text and library setup that reproduces a
mismatch.

## Focused acceptance pass

- Fresh state: open Add with no active session, tap Start or open workout, verify
  the workout sheet appears, start a workout, log a set, and finish it.
- Existing session: the same action opens the active workout; the bottom accessory
  still opens it, and the rest timer keeps accessory priority.
- Deep links: `marble://workout`, `marble://train`, and `marble://split` reach the
  workout surface whether or not a session is active.
- Duplicate names, selected existing exercise: create two library exercises with
  the same display name but different histories, select one explicitly in import
  review, save, and verify PR celebration evaluates only that UUID's history.
- Duplicate names, Create New: choose Create New while a same-named exercise has
  history, save, and verify Marble does not claim that unrelated history as a PR.
- Regression: natural language, notation, Notes, Hevy/Strong CSV, unresolved-line
  retry, draft recovery, Shortcut handoff, active-session Add Set, Log modes,
  Progress, widgets, backup/restore, and shipped-2.3 migration.
- Accessibility: Start or open workout has a truthful VoiceOver label and reachable
  hit target at default and maximum Larger Text in light and dark appearance.

## Exit criteria

- The processed build is tied to the exact committed GitHub SHA and is `VALID`.
- Internal `test group A` receives it through all-build access.
- Targeted reachability and exact-exercise-identity regressions pass.
- Full unit, UI, accessibility, snapshot, migration, archive/sign/export, and
  physical-device gates have no release-blocking regression.
- TestFlight acceptance evidence accompanies the current build 60 App Review
  submission; physical-device acceptance and soak remain separate.
- App Store 2.4 remains manual release; no public release is implied by upload,
  processing, internal testing, attachment, submission, or approval.

## App Store Connect commands

Internal `test group A` has all-build access; do not add the build manually or
trigger external Beta App Review. If the note changes, update the existing en-US
localization idempotently:

```sh
asc builds test-notes update \
  --build-id ef651ca3-451f-468b-903a-1239bcf6dc39 \
  --locale en-US \
  --whats-new "$(sed -n '/^## What to Test/,/^## Focused acceptance pass/p' AppStore/testflight/2.4-build-60.md | sed '1d;$d')"
```

App Store replacement completed: the old build 59 submission was canceled,
build 60 was attached to version 2.4, readiness was revalidated, and fresh
submission `5bd874ad-3619-4521-9231-fa45ee18a4b0` entered
`WAITING_FOR_REVIEW`. No public release has occurred.
